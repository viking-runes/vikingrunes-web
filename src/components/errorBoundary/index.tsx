import { Typography } from '@mui/material';
const ErrorBoundary = () => {
  return (
    <Typography variant={'h4'} style={{ padding: '3rem' }}>
      An error occurred!
    </Typography>
  );
};
export default ErrorBoundary;
